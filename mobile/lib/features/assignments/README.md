# assignments (not yet implemented)

docs/07 Phase 5. Lifecycle described in docs/08 §8.5 "Assignment lifecycle" — create → notify →
submit (resumable upload) → swipe-through review → feedback notification. Backed by
`backend/src/modules/assignments`.
